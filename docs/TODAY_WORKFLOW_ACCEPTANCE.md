# 今日工作優先改善（2026-09-10）

本批範圍：首頁、個案中心、出勤、生命徵象、照顧日誌、中央 HTML 匯入狀態說明。保留 89 個入口、品牌與載入動畫；不調整帳號、RLS、簽署或正式資料寫入授權。

## 驗收

- 首頁列出授權在案個案的尚無出勤、尚無量測、日誌待完成及需留意；統計和清單使用同一判斷。未接入排程前不稱為當日應服務名單或完成率。
- 請假／未到不自動要求新增量測、日誌；仍可查閱既有紀錄。無權限不偽裝為零或已完成。
- 個案選一次，出勤、量測、日誌帶入相同個案及服務日。無效、多值或不可查閱個案參數不得改成另一人。
- 表單未儲存時切換須避免無提示丟失；重送遵循原有冪等流程。
- 前線顯示缺少項目與下一步；技術資訊集中管理檢查區，不隱藏錯誤、權限或簽署風險。
- HTML 解析、暫存核准、正式入檔分開。正式儲存、逐欄映射、WORM 未驗證前不得宣稱已正式匯入。
- 合成資料驗證 1440px、390px，無整頁橫向溢出、觸控至少 44px、輸入至少 16px，鍵盤可達、狀態不僅靠顏色。

## 證據與限制

修改前畫面：`/tmp/daycare-today-ux.sCJ1gK/before-*.png`。安全展示伺服器只綁本機且停用外部整合。不讀寫真實個案，不代表正式人員操作、醫療／申報或全 89 頁驗收通過。發佈狀態依完成後實際紀錄為準。

## 本批實作結果

| 範圍 | 結果 |
| --- | --- |
| #1 今日工作 | 以授權在案名單列出四種待核對工作，數字與名單同一判斷；搜尋、20 人分頁、更新保留篩選；離線提示可見。具體工作篩選的按鈕直達該工作，預設清單才依需留意／出勤／量測／日誌排序。 |
| #2 個案中心 | 移除不可用的假新增入口；有效在案且服務中的個案、具頁面權限時可開始當日工作，其餘僅可到授權的查閱頁；保留篩選、分頁、捲動與返回焦點。 |
| #3、#6、#46 | 個案選定後顯示三步接續與新增按鈕，表單只收到該個案；日期與 ID 沿用，無效或多值參數先阻擋，不同個案不會被默默代入。單人統計隱藏，技術與驗證細節後移。 |
| 寫入回覆 | 量測和日誌新增完整 JSON 回執核對：模式、保存狀態、筆數／種類或草稿版本不符時保留內容與原重試鍵。出勤沿用原有嚴格回執檢查。API、SQL、RLS、簽署與 AAL2 規則未改。 |
| #80 中央 HTML | 清楚分成解析、核對／暫存核准、正式入檔尚未完成；來源雜湊、契約移至管理明細，警示與未知欄位保留可見。未實作正式提升交易。 |

## 最終本機檢查（2026-09-10）

- `pnpm lint`、`pnpm typecheck`、`pnpm build`、`git diff --check`：通過。
- Vitest：312 檔、3,155 測試通過。
- PGlite：97 migrations 編譯通過；95 檔、4,004 斷言通過，其中 2 套執行長模式、93 套明列的舊角色合成測試模式。這是本機相容性測試，不是正式 PostgreSQL 或雲端並行測試。
- 5 個主要頁面 1440／390px：沒有整頁橫向溢出，檢查到的主要操作目標至少 44px、輸入至少 16px；已選個案的量測操作、未儲存 Escape 確認及成功後焦點返回驗證通過。
- 首頁四卡數字／名單為 1／2／2／1，預設待處理去重 5 人；搜尋及更新保留、離線狀態、非法日期、多值參數、未知個案不替換他人均已驗證。
- #80：僅用 `tests/fixtures/imports/synthetic-central.html`，上傳→預覽成功；兩筆既有 POST／GET，惡意外部網址請求 0；390px、管理明細 Enter 開關與未知欄位篩選驗證通過。
- 同一合成個案在瀏覽器依序完成出勤、量測、日誌草稿的實際展示 API；三次回覆均為 `persisted: false`，日誌維持 `draft`，client/date 沿用，沒有外部請求或頁面錯誤。這不是正式資料持久化驗收。
- 量測／日誌回覆缺 body、非 JSON、錯模式、錯筆數／種類或錯紀錄的合成回歸通過；未驗證真實個案持久化。
- 建置仍有既存 `preferredRegion` route-segment 棄用警告；本批未變更區域配置，後續部署前另確認現行平台設定。

## 未完成／不能據此宣稱的項目

1. **尚未發佈**：本批僅在隔離工作分支 `codex/today-workflow-20260910` 完成，未推送 GitHub、未改 Vercel／Supabase／AWS 或正式網站。
2. **今日排程仍未接入**：目前是授權在案工作核對清單，不是當日應到名單。已防止未到收案生效日的個案進入日常工作；歷史生命週期快照與排程口徑不是本批完成項目。
3. **中央 HTML 正式入檔未完成**：詳 `CENTRAL_IMPORT_PROMOTION_GATES.md`；需要核准欄位字典、穩定識別與缺值規則、原始檔七年防刪封存、正式交易及對帳。不能把解析／暫存核准改字為正式成功。
4. **未送出草稿只在記憶體**：取消、Escape、遮罩、一般連結、GET 日期切換、整頁離開有提醒／阻擋；App Router 的 SPA 瀏覽器「上一頁」尚無可靠可取消攔截，請先保存再使用。沒有新增本機個資快取或宣稱離線草稿完整。
5. 全 89 頁正式業務驗收、50 人壓測、全 WCAG AA／螢幕閱讀器、真實人員 Google 工作階段及正式入檔／簽署／申報驗收均未在本批執行。

## 本機視覺證據

- 比較板：`/tmp/daycare-today-ux.sCJ1gK/comparison.html`，首屏比較圖 `comparison-mobile.png`。
- 手機選定個案：`after-vitals-selected-390.png`、`after-vitals-form-390.png`。
- 獨立覆核：同目錄 `review-*.png`；匯入頁前後圖在 `/tmp/daycare-import80-ui.hnGIFI/`。
- 瀏覽器驗證腳本：`/tmp/daycare-today-ux.sCJ1gK/verify-ui.mjs`；僅准本機網路，正式環境不得執行此合成腳本。

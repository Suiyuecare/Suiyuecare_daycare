# 第一批：實作與驗證紀錄

日期：2026-09-13。狀態：**01–03 部分程式完成、本機測試通過；未發布、未開通正式帳號、未核准承載真實個案資料。**

本紀錄對照 `USER_CENTERED_COMPLETION_PLAN_2026-09-13.md` 的工作包，不代表三包或全 89 頁均完成。保留既有 89 頁及單店出勤／Finance 收支入口，未新增付費服務、變更雲端區域或修改 Finance。

## 本次可交付的改善

| 範圍 | 實作內容 | 已取得的證據 |
| --- | --- | --- |
| 01 個人 Google 一般作業 | 新增逐人固定 Google 身分核准底座；一般出勤、生命徵象與照顧日誌草稿／未簽署提交，明確 opt-in 例行授權。不把 Google AAL1 偽裝為 AAL2，不按網域自動放行。 | 應用程式登入、API、SQL 正反測試；未以正式帳號寫入。 |
| 02 主管準備清單 | 首頁提供主管入口；機構頁列機構、人員、資格、個案、聯絡、當班、照顧依據、安全交付及備援九項，分開缺項／無法核對／待人工確認，提供負責角色及補正入口。 | 26 項準備清單測試、瀏覽器 390px／1440px。沒有人工勾選就冒充核准上線的功能。 |
| 03 接續工作 | 今日清單選上午／下午後，個案、日期與班別接續至出勤、生命徵象及日誌；日誌表單帶入所選班別。當日證據不等於本班完成。 | 連結、表單、日期、元件測試及修改前後瀏覽器比較。 |
| 03 不確定結果與重試 | 日誌操作送出不確定時凍結原內容／版本／冪等鍵，只重試原操作；收據欄位與實際重讀分開驗證。不因 HTTP 200 或回應缺欄位而宣稱完成。 | 網路失敗、錯誤收據、缺讀回、版本／重送元件測試。 |
| 03 遺失出勤回應 | 已寫入的出勤超過 15 分鐘才重送，同內容與鍵仍由交易取回原回執；新補登仍由原主管／近期驗證規則拒絕不合格操作。 | 實際 API handler 測試（mock Auth/RPC）及 SQL 冪等／補登測試。 |
| 必要權限接線 | 新核准人員可讀自己的有效指派，支援個案中心「我負責」；新血糖寫入未開放時先顯示唯讀提示、停用按鈕。 | 自我指派隔離 SQL 測試、血糖唯讀元件測試。 |

核心工作區另增加「即使收到未篩選快照，也只顯示選定個案」的元件防呆。基線頁面原本已在上游篩選，因此本次瀏覽器前後均是一位個案；不能用元件測試宣稱正式頁曾出現跨個案外洩。

## 精確驗證結果

- `pnpm lint`：通過，無警告。
- `pnpm typecheck`：通過。
- `pnpm test`：352 檔通過、1 檔跳過；3,733 項通過、1 項跳過。未將 skipped 算成通過。
- 最終 SQL：103 份遷移編譯，100 檔、4,434 assertions 通過；新增授權 suite 206／206。
- `pnpm build`：通過。仍有既有 `preferredRegion` 棄用提醒；本次未變更區域或方案。
- `git diff --check`：通過。
- 7 個改動／接續頁面在 390px、1440px 的修改前後，共 28 張頁面截圖；另有選定個案與日誌表單 8 張。完成後無整頁橫向溢位、無 Next 錯誤遮罩、無未完成載入層。
- 合成資料實際操作：今日工作選下午 → 選指定個案 → 日誌表單帶入該人與下午 → 儲存草稿，畫面明確回報展示資料不永久保存。這不是正式持久化成功證據。
- 準備清單在瀏覽器模擬離線時顯示「不能視為最新」，停用重新檢查；恢復連線後可以重新核對。這不是離線寫入或真實備援演練。

SQL runner 有 93 份歷史 PGlite-only admission fixtures、7 份執行真實遷移 admission predicate 的 suites。PGlite 不等於正式 Supabase PostgreSQL／真正 Google Session／並行交易驗收；API mocks、瀏覽器展示與 SQL 分別通過，也不能合稱正式端到端驗收。

## 畫面與無障礙

[修改前後比較板](qa/2026-09-13-phase1/comparison.html) 包含同一日期、合成資料與視窗的實際截圖，非設計示意。基線為 c5716bb（應用程式來源 4064291），沒有拿修改後的頁面冒充 Before。

基線隔離目錄最初因複用相依套件超出 Turbopack root 而無法啟動；保留原始碼不變，改用相同 Next 版本的 webpack 本機展示模式取得 Before。After 仍以 Turbopack 驗證。這些截圖只證明畫面與互動，不比較編譯器效能。

依 UI/UX 簡化技能，本次保留原 Finance 配色、外框與載入動畫，將準備檢查放在主管入口；照服員的主流程仍是個案與下一件工作。新增分組 ARIA 語意、狀態文字及錯誤後可恢復路徑。未重新設計全部 89 頁。

axe 4.12.1 自動檢查：登入頁 0 violations、其餘六頁各有既有共用選單／麵包屑色彩對比 violations；漸層對比仍有 incomplete 項目。詳見 [檢查原始摘要](qa/2026-09-13-phase1/a11y-summary.json)。**不得標示已符合完整 WCAG 2.2 AA**；需另完成共用視覺對比調整及人工鍵盤、螢幕閱讀器／觸控驗收。

## 帳號與正式資料

使用者指定先處理既有執行長及另一個公司帳號。尚未為任何人建立本批 routine grant、Auth 使用者、邀請、角色或分支指派；第二個帳號仍缺實際使用人及職務，不設成共用登入，也不猜成分支最高權限。

唯讀核對確認原執行長的 Google 身分郵件已驗證、subject 與已核准身分相符，但沒有 `hd`。因此規則不把付費 Google Workspace 當成新增前提；缺 `hd` 不能取代其他固定身分證據，提供錯誤 `hd` 仍拒絕。查詢未輸出任何 Google subject、Token、原始身分資料或個案內容。

## 本批仍未完成的門檻

1. 正式 PostgreSQL／Supabase RLS、安全 advisor、多工作階段與代表角色實機驗證。沒有本機 PostgreSQL 服務，因此未取得 advisor 通過證據。
2. 完整員工邀請、逐人啟用、權限擴張雙人核准與可辨識操作人的管理 UI。目前授權底座由受控維運端操作，無 Auth 操作者時稽核明示 system actor。
3. 實際帳號透過 Google 登入後，三種例行紀錄真正寫入並重整／第二裝置讀回，以及停用後工作階段拒絕。既有 CEO 的一般 AAL1 寫入也需明確新核准。
4. 各專業與高風險操作的新身分驗證設計。新員工即使 AAL2 也不會自動獲准原 CEO 限定的高風險鏈；不可把一般草稿開放說成全角色、全模組開放。
5. 開站清單各項真實資料與責任人證據、正式個案名冊／有效照顧依據、資格、備份還原、安全交付及業務簽核。彙總來自各自來源讀取，不宣稱跨來源單一交易快照。
6. 完整離線 24 小時、關頁再開、跨分頁競態及真正重連 60 秒同步驗收。這次日誌 lifecycle 不確定操作狀態只在元件記憶體保留；不能承諾簽署等重要操作可離線或關頁後自動恢復。
7. CMS 注意事項的新一般員工讀取路徑仍未擴大。無法讀取會顯示未知／失敗，不代表沒有注意事項；正式 CMS 匯入與候選覆核屬工作包 04–06。
8. 原計畫 04–16 的其餘施工、正式 Finance／通知／IoT／移轉與全站驗收；本次沒有完成或啟用這些工作包。

## 發布先後與回復

**尚未執行發布。** 完成上述必要的安全／角色測試及核准後，先套用資料庫遷移並驗證新 RPC，才可發布依賴 `is_staff_login_allowed` 的應用程式；缺 RPC 會安全拒絕登入，不能前端先上。

正式啟用必須逐人核對固定身分、role／branch／個案指派後再核准 grant，且以安全測試資料驗證成功與拒絕路徑；公司網域不能自動取得權限。發生問題時先撤銷本批 grant、驗證拒絕，再回復應用程式；保留稽核及已保存業務資料，不刪表回滾。細節見 [權限交付紀錄](APPROVED_STAFF_ROUTINE_ACCESS_2026-09-13.md)。

# 日照系統補強與驗收紀錄 — 2026-09-22

## 範圍與狀態

本批從 `c77047d` 建立 `codex/daycare-completion-20260922`，接續既有日照系統，不修改 Finance。
以下紀錄區分實作、合成資料驗證與正式營運驗收。尚未提交、套用正式 Supabase migration、推送或部署；不宣稱 89 頁全部完工。
所有測試個案皆為合成資料，未使用使用者提供的真實 CMS HTML、附件或正式帳號進行寫入測試。

## 本批實作與可重現驗收

| 功能 | 使用者可見改變 | 必須通過的驗收 |
|---|---|---|
| 今日照顧適用名單 | 每週安排、當日例外、服務資格與實際到站合併判定；未安排另列待確認，請假／未到不誤算照顧漏填 | 非服務日不產生假待辦；臨時實到納入；歷史資料保留但不冒充本日；卡片與明細同快照；逐案來源授權；臺北日界；缺 RPC 時不退回不準確分母 |
| 核准 Google 帳號的日常操作 | 正式收案與每日人員分工不再強制先取得 AAL2 | 僅核准且有效的 Google grant/session；保留角色、分支、逐案範圍、版本與冪等；停用、撤權後重送拒絕；其他生命週期操作與正式簽署未被一併降級 |
| 已發布趟次取消 | 有理由取消有效發布版，接送需求回到待派；保留舊版本及取消人／時間 | 有新草稿仍能精確取消有效發布版；取消／開始執行不可同時成功；已開始或完成拒絕取消；取消後不再佔車、駕駛或個案衝突；等待鎖期間撤銷逐案授權也拒絕，包括收據重送 |
| 機構自訂表單填答 | 新增 `/app/client-forms`；照顧三步驟與表單治理都有入口，選定個案可接續；支援草稿、簽署、更正及歷史 | 只使用已發布且有效的 `tenant.custom.*` 版本；未填／不適用／false／0 分離；真正日期與範圍驗證；簽署使用已保存內容且凍結；更正保留原簽署；未知結果固定原 key/body 並阻止切換；同基準版本競態僅一筆成功 |

自訂表單不是 ADL/IADL 等官方量表的替代品，不新增醫療計分或照顧決策。範本停用／新版治理、填答 PDF 列印、附件與離線填答仍不在本批完成範圍。

## 測試入口

- `pnpm exec vitest run --maxWorkers=2`：完整應用回歸；首輪預設並行曾使 3 項既有 ABC UI 測試逾時，單獨重跑 11/11 通過；不可把逾時當成功。
- `node scripts/test-database.mjs`：全 migration 與本機 pgTAP。
- `INTAKE_NATIVE_PG_BIN=<Postgres17-bin> node scripts/test-transport-cancellation-native.mjs`：兩條真實資料庫連線的取消／執行／撤權競態。
- `CUSTOM_FORM_NATIVE_PG_BIN=<Postgres17-bin> node scripts/test-custom-form-responses-native.mjs`：同鍵與同基準版本競態。
- `pnpm lint`、`pnpm typecheck`、`pnpm build`、`git diff --check`。
- `pnpm dev:demo --port 3107`，另執行 `ROUTE_SMOKE_BASE_URL=http://127.0.0.1:3107 node scripts/test-page-routes.mjs`：89 頁路由煙霧測試，**不等於 89 頁作業／權限／正式資料驗收**。
- `node scripts/verify-completion-browser.mjs`：僅連本機合成展示，7 頁 × 390/1440px，檢查標題、單一 main、橫向溢出與錯誤遮罩，輸出暫存截圖與 JSON。

正式權限仍由 RPC 的即時授權決定；畫面按鈕或本機測試不構成正式授權證據。

### 本次已取得證據

- 全套 Vitest：418 個檔案、4,635 項通過，1 項跨 Finance repository 的選用整合測試未啟用；不把未啟用測試計入通過。
- 本機資料庫：123 個 migrations 編譯；整批 119 個 SQL 檔案、5,450 項 assertions 通過。其中 93 組保留既有多角色 PGlite fixture、26 組使用真實登入限制模型，並非所有舊測試都等於正式 Google 登入驗證。此後針對表單等待期間撤權及分頁同快照追加加固，自訂表單 SQL 最終為 56/56；未將增加的這一項回報為已重跑的全套 5,451 項。
- 真實 PostgreSQL 17.11 本機雙連線：接送取消 5 種競態（含撤權等待）；新表單 4 種競態（同鍵、同基準版本、等待期間撤權後寫入／重送），全部通過。授權 fixture 仍為合成，並非正式 Supabase 使用者。
- 最後加固後另跑自訂填答、核准 Google 日常照顧、表單建置與發布 4 組 SQL 回歸：379/379 通過，123 個 migrations 再次編譯通過。
- 89/89 路由可開啟；本批 7 頁 × 2 種寬度共 14/14 檢查通過。另實際點擊量測頁的個案表單入口、開啟合成填答，確認 0／否／不適用獨立呈現，欄位字體 16px、控制項約 46.8px、單一主要區域且無橫向溢出。
- ESLint、TypeScript、正式建置與 diff 空白檢查通過。尚未執行第三方弱掃、完整螢幕閱讀器驗收、50 人壓測或正式帳號端到端作業。

瀏覽器證據保存在本機暫存目錄 `daycare-browser-acceptance-jtKClw`，可用上列腳本重新產生，沒有將正式個案內容存入版本庫。

## 仍未完成的工作及解除條件

| 優先序 | 尚待完成 | 驗收門檻／所需資料 |
|---|---|---|
| P0 | 正式 CMS 原始檔封存、附件掃毒與真人收案全流程 | 核定封存方案、保留／刪除政策及掃毒服務；不得把普通私有 Storage 宣稱為七年 WORM。以去識別化檔案驗證上傳→隔離→解析→衝突覆核→單一交易建檔→每週安排→交通→文件→正式收案；失敗零正式變更。未配置時不可解除上傳保護。 |
| P0 | 官方評估表完整題目、公式與版本 | 提供機構現用 ADL、IADL、吞嚥、BSRS 及適用版本；沿用已提供臺北 ABC PDF，但需逐欄對照及真人驗收；固定答案／缺值／不適用結果全部符合來源，雙人核准發布。不得用推測公式填滿頁面。 |
| P0 | 官方申報匯出及回覆對帳 | 提供去識別化現用年度／縣市匯出及回覆樣本、費率／代碼；逐欄、逐筆、總額完全一致，重複申報為零；目前的內部快照不可冒稱官方申報檔。 |
| P0 | 正式環境及帳號寫入驗收 | 現行部署版本／migration／環境配置重新核對；實際李佳泰與萬華負責人帳號按角色操作，測跨分支拒絕、撤權、登入與重要操作。不得以展示環境或管理金鑰替代使用者測試。 |
| P1 | 單店 Finance 收支接通 | 確認唯讀端點、店別 mapping、授權與統計口徑；店／期間金額與 Finance 完全一致，不擴張成日照端財務寫入。 |
| P1 | 家屬正式資料及 LINE 通知閉環 | 完成逐個案／類別同意與到期撤銷、實際資料讀取、公司 LINE provider 配置；跨家屬零外洩，重送不重複，推播不含敏感健康值；無線上付款。 |
| P1 | 共用能力的逐頁覆蓋 | 量表版本、提醒處置、附件、列印／匯出、表單新版／停用、離線範圍等需依頁補驗。89 頁路由全通不解除這些缺項。 |
| P1 | 七年移轉、備援及容量驗收 | 實際來源清冊、兩次移轉彩排、關鍵欄位／金額／附件核對；500 個案／50 並行、七年量測，備份還原與故障切換、平行完整申報週期；不得由單元測試推論 SLA/RPO/RTO 已達成。 |
| P2 | IoT 真實設備串接 | 選定設備、資料協定、設備－個案配對與供應商；驗單位、時間、同鍵去重、錯配隔離、斷線／補送／撤綁；未驗證資料不得自動簽署或改變照顧決策。 |

## 正式發布前順序

1. 核對遠端最新分支與目前部署，保留其他人未提交修改；這份本機分支不能直接假設與正式版一致。
2. 完成備份／回復點、migration dry-run 與本批 SQL 權限驗收，核准後先套 4 個新增 migration，再上應用程式；今日名單依賴新 RPC，缺少時刻意停止載入。
3. 預覽環境通過核准帳號的實際讀寫與手機流程，確認正式環境變數、快取／區域及日誌不洩漏個資。
4. 正式發布後核對 deployment/source 版本，再驗收收案、今日工作、表單與取消接送；若須回復，以應用版本回復為優先，不刪除不可變紀錄表或取消歷史。
5. 由機構負責人完成業務驗收；未過的官方規格、外部服務與災難復原門檻繼續列為未完成，不以交付程式碼取代。
